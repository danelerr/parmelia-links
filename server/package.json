{
  "name": "server",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.12.5",
    "jose": "^6.2.0",
    "viem": "^2.47.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "vitest": "^4.1.8",
    "wrangler": "^4.4.0"
  }
}